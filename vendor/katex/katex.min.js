// Minimal stub for KaTeX runtime placeholder.
// For full rendering use the upstream KaTeX build.
window.katex = window.katex || {
    render: function(tex, node, options){
        // Very naive fallback: escape and show TeX inside a code block
        node.innerText = tex;
    }
};
